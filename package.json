{
  "name": "aws-leaderboard-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "AWS SAM serverless leaderboard application with user authentication and real-time notifications",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy:local:backend": "npm run deploy:local --workspace=@aws-sam/backend",
    "dev:frontend": "npm run dev --workspace=@aws-sam/frontend",
    "build:all": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\""
  },
  "keywords": [
    "aws",
    "sam",
    "serverless",
    "leaderboard",
    "authentication",
    "websockets",
    "monorepo",
    "lambda",
    "dynamodb",
    "cognito"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.8.0"
  },
  "packageManager": "npm@10.0.0"
}
